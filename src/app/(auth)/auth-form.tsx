"use client";import Link from"next/link";import{useActionState}from"react";import{CircleDollarSign,LoaderCircle}from"lucide-react";import type{AuthState}from"./actions";
export function AuthForm({mode,action}:{mode:"login"|"signup";action:(s:AuthState,d:FormData)=>Promise<AuthState>}){const[state,formAction,pending]=useActionState(action,{});const enter=mode==="login";return <main className="auth-page"><section className="auth-panel"><div className="auth-brand"><CircleDollarSign/><strong>BS Financeiro</strong></div><div><p className="eyebrow">{enter?"BEM-VINDO DE VOLTA":"COMECE AGORA"}</p><h1>{enter?"Entre na sua conta":"Crie sua conta"}</h1><p className="muted">Seu dinheiro explicado de forma simples.</p></div><form action={formAction} className="auth-form"><label htmlFor="email">E-mail</label><input id="email" name="email" type="email" autoComplete="email" required/><label htmlFor="password">Senha</label><input id="password" name="password" type="password" autoComplete={enter?"current-password":"new-password"} minLength={8} required/><button disabled={pending}>{pending?<LoaderCircle className="spin"/>:null}{enter?"Entrar":"Criar conta"}</button>{state.error?<p className="form-error" role="alert">{state.error}</p>:null}{state.success?<p className="form-success" role="status">{state.success}</p>:null}</form><p className="auth-switch">{enter?"Ainda não tem conta?":"Já tem uma conta?"} <Link href={enter?"/cadastro":"/entrar"}>{enter?"Cadastre-se":"Entrar"}</Link></p></section><aside className="auth-aside"><blockquote>“Finalmente consigo entender para onde meu dinheiro está indo.”</blockquote><p>Controle semanal, visual e sem planilhas complicadas.</p></aside></main>}
