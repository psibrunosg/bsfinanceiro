import{NextResponse,type NextRequest}from"next/server";import{createClient}from"@/lib/supabase/server";export async function GET(r:NextRequest){const code=r.nextUrl.searchParams.get("code");if(code){const s=await createClient();const{error}=await s.auth.exchangeCodeForSession(code);if(!error)return NextResponse.redirect(new URL("/",r.url));}return NextResponse.redirect(new URL("/entrar?erro=confirmacao",r.url));}
